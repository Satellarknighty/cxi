import java.util.Scanner;

/**
 * To parse the input of users like a pro, but I'm too lazy.
 */
public class Parser {

    private static Scanner scanner;

    public Parser(){
        scanner = new Scanner(System.in);
    }
}
